{
  "name": "xelis-payment-block",
  "version": "0.2.7",
  "description": "",
  "private": "true",
  "scripts": {
    "build-block": "esbuild ./block.js --loader:.js=jsx --external:react --bundle --outdir=build",
    "build-classic": "esbuild ./classic.js --loader:.js=jsx --external:react --bundle --outdir=build",
    "start-block": "npm run build-block -- --watch",
    "start-classic": "npm run build-classic -- --watch"
  },
  "author": "g45t345rt",
  "license": "ISC",
  "dependencies": {
    "pretty-ms": "^9.1.0",
    "qrcode.react": "^4.0.1",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
